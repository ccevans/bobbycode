---
description: "Define the product — brief → personas → journeys → feature map, with a gate at each stage"
argument-hint: "<epic ID, e.g. TKT-001>"
---

Load and follow the skill in `.claude/skills/bobby-define/SKILL.md` to take the specified epic through product definition (brief → personas → journeys → feature map → blueprint). Resume from whatever artifacts already exist in `.bobby/product/`.
