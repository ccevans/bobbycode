{
  "name": "web",
  "display": "Static web page (built-in starter)",
  "health_checks": [
    {
      "name": "app",
      "url": "http://localhost:3000",
      "description": "Static file server"
    }
  ],
  "areas": [
    "ui"
  ],
  "commands": {
    "dev": "node server.js",
    "test": "node --test",
    "lint": "",
    "build": ""
  },
  "testing_tools": [
    "curl"
  ],
  "template_vars": {
    "test_command": "node --test",
    "lint_command": "",
    "spec_dir": "test/"
  }
}
