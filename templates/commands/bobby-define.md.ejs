---
description: "Define the product — brief → personas → journeys → data model → architecture (both optional) → feature map → mockups (optional) → blueprint, with a gate at each stage"
argument-hint: "<epic ID, e.g. TKT-001>"
---

Load and follow the skill in `<%= paths.skills %>/bobby-define/SKILL.md` to take the specified epic through product definition (brief → personas → journeys → data model → architecture (both optional) → feature map → mockups (optional) → blueprint). Resume from whatever artifacts already exist in `.bobby/product/`.
