---
description: "Run the full SDLC pipeline on tickets — plan → build → review → test"
argument-hint: "<ticket ID(s)>"
---

Load and follow the skill in `.claude/skills/bobby-pipeline/SKILL.md` to run the pipeline on the specified ticket(s).
