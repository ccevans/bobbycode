{
  "scripts": {
    "setup": "<%= commands && commands.dev && commands.dev.startsWith('docker') ? 'docker compose build' : '' %>",
    "run": "<%= commands && commands.dev ? commands.dev : '' %>"
  }
}
