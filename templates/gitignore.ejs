# Bobby — session logs (not committed)
.bobby/sessions/

# Bobby — a live claim on a ticket, never a fact about it (BOB-120). Committed,
# it would block that ticket on every other machine until the staleness ceiling.
.bobby/tickets/*/run.lock

# Dependencies
node_modules/
vendor/
<% if (stack === 'python-flask') { -%>
__pycache__/
*.pyc
.venv/
venv/
<% } -%>

# Build output
dist/
build/
<% if (stack === 'nextjs') { -%>
.next/
out/
<% } -%>
<% if (stack === 'python-flask') { -%>
*.egg-info/
<% } -%>

# Environment
.env
.env.local
.env.*.local

# OS
.DS_Store
Thumbs.db
